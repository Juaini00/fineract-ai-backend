info:
  name: session contoh Indonesia withdrawal breakdown
  type: http
  seq: 17

http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{retrievalSelectionToken}}"
  body:
    type: json
    data: |
      { "title": "retrieval-selection withdrawal breakdown id" }

runtime:
  scripts:
    - type: tests
      code: |
        test("201", function () { expect(res.getStatus()).to.equal(201); });
        bru.setVar("withdrawalBreakdownIdSessionId", res.getBody().data.id);
