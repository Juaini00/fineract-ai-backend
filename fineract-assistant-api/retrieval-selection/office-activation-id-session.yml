info:
  name: session contoh Indonesia office activation
  type: http
  seq: 11

http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{retrievalSelectionToken}}"
  body:
    type: json
    data: |
      { "title": "retrieval-selection office activation Indonesia" }

runtime:
  scripts:
    - type: tests
      code: |
        test("201", function () { expect(res.getStatus()).to.equal(201); });
        bru.setVar("officeActivationIdSessionId", res.getBody().data.id);
