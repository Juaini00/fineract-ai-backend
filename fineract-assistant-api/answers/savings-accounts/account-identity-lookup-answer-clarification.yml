info:
  name: savings_account_identity_lookup jawab klarifikasi
  type: http
  seq: 1.5
http:
  method: post
  url: "{{baseUrl}}/chat/jobs/{{acctIdentityJob}}/responses"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{answersToken}}"
    - name: Idempotency-Key
      value: "{{acctIdentityKey}}-answer"
  body:
    type: json
    data: |
      {
        "clarification_id": "{{acctIdentityClarificationId}}",
        "revision": {{acctIdentityRevision}},
        "answers": { "savings_account_id": "{{acctIdentityOptionId}}" }
      }
runtime:
  scripts:
    - type: tests
      code: |
        test("jawaban klarifikasi durable, job yang sama kembali mengantre", function () {
          expect(res.getStatus()).to.equal(202);
          expect(res.getBody().data.job_id).to.equal(bru.getVar("acctIdentityJob"));
          expect(res.getBody().data.lifecycle).to.equal("Queued");
        });
