info:
  name: savings_pending_charges_clients session
  type: http
  seq: 7.1
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{answersToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-52 savings_pending_charges_clients" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("pendingChargesSession", res.getBody().data.id);
