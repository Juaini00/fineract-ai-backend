info:
  name: savings_account_identity_lookup answer
  type: http
  seq: 1.6
  description: |
    FIN-137 — savings_account_identity_lookup sekarang terjangkau:
    account_number resolves ke savings_account_id lewat
    savings_account_identity_resolve (K1), dan jawabannya dibandingkan
    dengan SQL langsung (tests/answers/savings_account_identity_lookup.sql).
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{acctIdentityJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (await answers.awaitResponse(bru, req, res)) {
          answers.check(test, expect, "savings_account_identity_lookup", res.getBody().data);
        }
