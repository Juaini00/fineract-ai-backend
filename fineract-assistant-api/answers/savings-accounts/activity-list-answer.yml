info:
  name: savings_activity_list answer
  type: http
  seq: 6.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{activityListJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "savings_activity_list answer")) {
          answers.check(test, expect, "savings_activity_list", res.getBody().data);
        }
