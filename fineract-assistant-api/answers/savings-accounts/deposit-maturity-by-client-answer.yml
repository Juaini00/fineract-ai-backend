info:
  name: savings_deposit_maturity_by_client answer
  type: http
  seq: 10.6
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{depositMaturityJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, req, res)) {
          answers.check(test, expect, "savings_deposit_maturity_by_client", res.getBody().data);
        }
