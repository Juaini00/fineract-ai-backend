info:
  name: savings_activity_list session
  type: http
  seq: 6.1
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{answersToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-52 savings_activity_list" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("activityListSession", res.getBody().data.id);
