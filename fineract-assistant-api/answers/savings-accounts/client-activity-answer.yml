info:
  name: savings_client_activity answer
  type: http
  seq: 9.6
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{clientActivityJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "savings_client_activity answer")) {
          answers.check(test, expect, "savings_client_activity", res.getBody().data);
        }
