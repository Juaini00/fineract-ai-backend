info:
  name: savings_pending_charges_clients answer
  type: http
  seq: 7.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{pendingChargesJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, req, res)) {
          const document = res.getBody().data;
          answers.check(test, expect, "savings_pending_charges_clients", document);
          // FIN-133: hard_cap=10000 -> baris dipotong ke 10000; completeness
          // Partial + limitation row_cap_reached hanya bila populasi charge
          // pending > 10000.
          answers.expectRowCap(
            test,
            expect,
            "savings_pending_charges_clients",
            document,
            10000,
            answers.populationSize("savings_pending_charges_clients")
          );
        }
