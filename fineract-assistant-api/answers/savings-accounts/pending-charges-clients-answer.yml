info:
  name: savings_pending_charges_clients answer
  type: http
  seq: 7.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{pendingChargesJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "savings_pending_charges_clients answer")) {
          answers.check(test, expect, "savings_pending_charges_clients", res.getBody().data);
        }
