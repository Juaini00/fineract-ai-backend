info:
  name: savings_strictly_overdue_charges_clients answer
  type: http
  seq: 8.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{overdueChargesJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "savings_strictly_overdue_charges_clients answer")) {
          answers.check(test, expect, "savings_strictly_overdue_charges_clients", res.getBody().data);
        }
