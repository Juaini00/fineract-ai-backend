info:
  name: savings_products_by_client jawab klarifikasi
  type: http
  seq: 11.5
http:
  method: post
  url: "{{baseUrl}}/chat/jobs/{{productsByClientJob}}/responses"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{answersToken}}"
    - name: Idempotency-Key
      value: "{{productsByClientKey}}-answer"
  body:
    type: json
    data: |
      {
        "clarification_id": "{{productsByClientClarificationId}}",
        "revision": {{productsByClientRevision}},
        "answers": { "client_id": "{{productsByClientOptionId}}" }
      }
runtime:
  scripts:
    - type: tests
      code: |
        test("jawaban klarifikasi durable, job yang sama kembali mengantre", function () {
          expect(res.getStatus()).to.equal(202);
          expect(res.getBody().data.job_id).to.equal(bru.getVar("productsByClientJob"));
          expect(res.getBody().data.lifecycle).to.equal("Queued");
        });
