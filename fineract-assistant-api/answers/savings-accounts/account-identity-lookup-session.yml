info:
  name: savings_account_identity_lookup session
  type: http
  seq: 1.1
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{answersToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-52 savings_account_identity_lookup" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("acctIdentitySession", res.getBody().data.id);
