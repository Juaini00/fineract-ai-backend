info:
  name: client_top_n_by_deposit_volume answer
  type: http
  seq: 4.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "client_top_n_by_deposit_volume answer")) {
          answers.check(test, expect, "client_top_n_by_deposit_volume", res.getBody().data);
        }
