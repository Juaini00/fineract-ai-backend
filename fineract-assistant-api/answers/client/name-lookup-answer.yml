info:
  name: client_name_lookup answer
  type: http
  seq: 3.5
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "client_name_lookup answer")) {
          answers.check(test, expect, "client_name_lookup", res.getBody().data);
        }
