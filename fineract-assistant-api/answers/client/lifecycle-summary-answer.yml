info:
  name: client_lifecycle_summary answer
  type: http
  seq: 10.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "client_lifecycle_summary answer")) {
          answers.check(test, expect, "client_lifecycle_summary", res.getBody().data);
        }
