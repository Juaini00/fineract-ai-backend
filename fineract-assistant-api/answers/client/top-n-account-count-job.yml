info:
  name: client_top_n_by_savings_account_count job
  type: http
  seq: 5.2
http:
  method: post
  url: "{{baseUrl}}/chat/jobs"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{answersToken}}"
    - name: Idempotency-Key
      value: "{{answersKey}}"
  body:
    type: json
    data: |
      { "session_id": "{{answersSession}}", "request_text": "Which clients have the most savings accounts?" }
runtime:
  scripts:
    - type: before-request
      code: |
        bru.setVar("answersKey", "fin52-client_top_n_by_savings_account_count-" + Date.now() + Math.random().toString(36).slice(2, 10));
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(202);
        bru.setVar("answersJob", res.getBody().data.job_id);
