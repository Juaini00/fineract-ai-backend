info:
  name: client_top_n_by_savings_balance answer
  type: http
  seq: 6.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "client_top_n_by_savings_balance answer")) {
          answers.check(test, expect, "client_top_n_by_savings_balance", res.getBody().data);
        }
