info:
  name: savings_deposit_total answer
  type: http
  seq: 2.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "savings_deposit_total answer")) {
          answers.check(test, expect, "savings_deposit_total", res.getBody().data);
        }
