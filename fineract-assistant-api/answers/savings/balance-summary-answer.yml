info:
  name: savings_balance_summary answer
  type: http
  seq: 1.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "savings_balance_summary answer")) {
          answers.check(test, expect, "savings_balance_summary", res.getBody().data);
        }
