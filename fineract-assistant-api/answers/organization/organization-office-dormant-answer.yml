info:
  name: organization_office_dormant answer
  type: http
  seq: 7.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (await answers.awaitResponse(bru, req, res)) {
          const document = res.getBody().data;
          answers.check(test, expect, "organization_office_dormant", document);
          // FIN-133: hard_cap=100 -> baris dipotong ke 100; completeness
          // Partial + limitation row_cap_reached hanya bila populasi kantor
          // dorman > 100.
          answers.expectRowCap(
            test,
            expect,
            "organization_office_dormant",
            document,
            100,
            answers.populationSize("organization_office_dormant")
          );
        }
