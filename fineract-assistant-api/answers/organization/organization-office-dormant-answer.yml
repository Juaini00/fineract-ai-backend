info:
  name: organization_office_dormant answer
  type: http
  seq: 7.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "organization_office_dormant answer")) {
          answers.check(test, expect, "organization_office_dormant", res.getBody().data);
        }
