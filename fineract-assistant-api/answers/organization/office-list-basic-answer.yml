info:
  name: office_list_basic answer
  type: http
  seq: 4.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "office_list_basic answer")) {
          answers.check(test, expect, "office_list_basic", res.getBody().data);
        }
