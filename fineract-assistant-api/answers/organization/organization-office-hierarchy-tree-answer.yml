info:
  name: organization_office_hierarchy_tree answer
  type: http
  seq: 8.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, req, res)) {
          const document = res.getBody().data;
          answers.check(test, expect, "organization_office_hierarchy_tree", document);
          // FIN-133: hard_cap=500 -> baris dipotong ke 500; completeness
          // Partial + limitation row_cap_reached hanya bila populasi node
          // pohon > 500.
          answers.expectRowCap(
            test,
            expect,
            "organization_office_hierarchy_tree",
            document,
            500,
            answers.populationSize("organization_office_hierarchy_tree")
          );
        }
