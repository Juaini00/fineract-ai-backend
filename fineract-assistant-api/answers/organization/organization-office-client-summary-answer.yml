info:
  name: organization_office_client_summary answer
  type: http
  seq: 6.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, req, res)) {
          const document = res.getBody().data;
          answers.check(test, expect, "organization_office_client_summary", document);
          // FIN-133: hard_cap=200 -> baris dipotong ke 200; completeness
          // Partial + limitation row_cap_reached hanya bila populasi office
          // > 200.
          answers.expectRowCap(
            test,
            expect,
            "organization_office_client_summary",
            document,
            200,
            answers.populationSize("organization_office_client_summary")
          );
        }
