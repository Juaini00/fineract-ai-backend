info:
  name: organization_office_savings_summary answer
  type: http
  seq: 10.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, res, "organization_office_savings_summary answer")) {
          answers.check(test, expect, "organization_office_savings_summary", res.getBody().data);
        }
