info:
  name: organization_office_opening_monthly_breakdown answer
  type: http
  seq: 9.3
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{answersJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{answersToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        const answers = require("./lib/answers.js");
        if (answers.awaitResponse(bru, req, res)) {
          answers.check(test, expect, "organization_office_opening_monthly_breakdown", res.getBody().data);
        }
