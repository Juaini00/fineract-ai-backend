info:
  name: entity-not-applied session
  type: http
  seq: 17

http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{paramsToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-135 entity not applied" }

runtime:
  scripts:
    - type: tests
      code: |
        test("201", function () { expect(res.getStatus()).to.equal(201); });
        bru.setVar("entityNotAppliedSessionId", res.getBody().data.id);
