info:
  name: period-id response
  type: http
  seq: 7

http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{periodIdJobId}}/response"
  headers:
    - name: authorization
      value: "Bearer {{paramsToken}}"

runtime:
  scripts:
    - type: tests
      code: |
        const polls = Number(bru.getVar("periodIdPolls") || 0);
        if (res.getStatus() === 404 && polls < 30) {
          bru.setVar("periodIdPolls", polls + 1);
          bru.setNextRequest(req.getName());
        } else {
          const document = res.getBody().data;
          const params = document.evidence_json.lineage[0].parameters;
          const fromDate = params.find((p) => p.name === "from_date");
          const toDate = params.find((p) => p.name === "to_date");
          test("FIN-135 period-id: '" + bru.getVar("periodIdRequestText") + "' binds the stated period (Indonesian month range), clamped to today", function () {
            expect(res.getStatus()).to.equal(200);
            expect(fromDate.value).to.equal(bru.getVar("periodIdExpectedFrom"));
            expect(toDate.value).to.equal(bru.getVar("periodIdExpectedTo"));
          });
          test("FIN-135 period-id: the parsed period is disclosed in a note block (D2)", function () {
            const note = document.blocks_json.find((b) => b.block_id === "slots_auto_bound");
            expect(note, "blok slots_auto_bound tidak ada").to.exist;
            const slot = note.auto_bound_slots.find((s) => s.field_id === "period");
            expect(slot, "slot period tidak diungkap").to.exist;
            expect(slot.provenance).to.equal("deterministic_parse");
          });
        }
