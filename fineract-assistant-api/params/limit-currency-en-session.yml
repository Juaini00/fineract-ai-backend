info:
  name: limit-currency-en session
  type: http
  seq: 14

http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{paramsToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-135 limit currency en" }

runtime:
  scripts:
    - type: tests
      code: |
        test("201", function () { expect(res.getStatus()).to.equal(201); });
        bru.setVar("limitCurrencyEnSessionId", res.getBody().data.id);
