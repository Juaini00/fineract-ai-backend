info:
  name: period-id session
  type: http
  seq: 5

http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{paramsToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-135 period id" }

runtime:
  scripts:
    - type: tests
      code: |
        test("201", function () { expect(res.getStatus()).to.equal(201); });
        bru.setVar("periodIdSessionId", res.getBody().data.id);
