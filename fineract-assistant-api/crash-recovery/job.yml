info:
  name: job yang worker-nya ditinggalkan sesudah query
  type: http
  seq: 3
http:
  method: post
  url: "{{baseUrl}}/chat/jobs"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{crashToken}}"
    - name: Idempotency-Key
      value: "{{crashKey}}"
  body:
    type: json
    data: |
      { "session_id": "{{crashSession}}", "request_text": "Show the savings portfolio summary." }
runtime:
  scripts:
    - type: before-request
      code: |
        bru.setVar("crashKey", "fin56-recovery-" + Date.now() + Math.random().toString(36).slice(2, 10));
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(202);
        bru.setVar("crashJob", res.getBody().data.job_id);
