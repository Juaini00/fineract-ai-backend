info:
  name: session untuk crash sesudah query
  type: http
  seq: 2
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{crashToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-56 crash after external call" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("crashSession", res.getBody().data.id);
