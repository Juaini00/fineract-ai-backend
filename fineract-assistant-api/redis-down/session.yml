info:
  name: session saat redis mati
  type: http
  seq: 3
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{redisDownToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-57 redis down" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("redisDownSession", res.getBody().data.id);
