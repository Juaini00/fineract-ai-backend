info:
  name: session untuk dataset terpotong
  type: http
  seq: 2
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{cappedToken}}"
  body:
    type: json
    data: |
      { "title": "DS-8.1 dataset capped" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("cappedSession", res.getBody().data.id);
