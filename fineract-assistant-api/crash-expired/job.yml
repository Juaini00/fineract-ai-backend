info:
  name: job yang kedaluwarsa saat attempt Running
  type: http
  seq: 3
http:
  method: post
  url: "{{baseUrl}}/chat/jobs"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{crashExpiredToken}}"
    - name: Idempotency-Key
      value: "{{crashExpiredKey}}"
  body:
    type: json
    data: |
      { "session_id": "{{crashExpiredSession}}", "request_text": "Show the savings portfolio summary." }
runtime:
  scripts:
    - type: before-request
      code: |
        bru.setVar("crashExpiredKey", "fin141-crashExpired-" + Date.now() + Math.random().toString(36).slice(2, 10));
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(202);
        bru.setVar("crashExpiredJob", res.getBody().data.job_id);
