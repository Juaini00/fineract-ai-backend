info:
  name: login untuk yang worker-nya selalu error sebelum admisi
  type: http
  seq: 1
http:
  method: post
  url: "{{baseUrl}}/auth/login"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |
      { "username": "{{username}}", "password": "{{password}}" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(200);
        bru.setVar("workerErrorToken", res.getBody().data.access_token);
