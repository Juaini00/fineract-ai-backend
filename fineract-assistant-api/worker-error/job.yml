info:
  name: job yang worker-nya selalu error sebelum admisi
  type: http
  seq: 3
http:
  method: post
  url: "{{baseUrl}}/chat/jobs"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{workerErrorToken}}"
    - name: Idempotency-Key
      value: "{{workerErrorKey}}"
  body:
    type: json
    data: |
      { "session_id": "{{workerErrorSession}}", "request_text": "Show the savings portfolio summary." }
runtime:
  scripts:
    - type: before-request
      code: |
        bru.setVar("workerErrorKey", "fin141-workerError-" + Date.now() + Math.random().toString(36).slice(2, 10));
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(202);
        bru.setVar("workerErrorJob", res.getBody().data.job_id);
