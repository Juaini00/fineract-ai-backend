info:
  name: assertion retrieval saldo nasabah Indonesia
  type: http
  seq: 23
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{indonesianClientBalanceJobId}}/response"
  headers:
    - name: authorization
      value: "Bearer {{engineToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        // FIN-146 — dibaca langsung sesudah create-job, tanpa poll job-state:
        // sumber flaky 404 asli tiket ini (bisa mendahului settlement job).
        const poll = require("./lib/poll.js");
        if (await poll.awaitJob(bru, req, res, () => true)) {
          test("FIN-40: saldo nasabah Indonesia memilih capability terindeks", function () {
            expect(res.getStatus()).to.equal(200);
            expect(res.getBody().data.evidence_json.lineage[0].capability_id)
              .to.equal("client_top_n_by_savings_balance");
          });
        }
