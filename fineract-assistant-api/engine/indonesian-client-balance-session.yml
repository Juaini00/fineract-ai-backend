info:
  name: session pertanyaan saldo nasabah Indonesia
  type: http
  seq: 21
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{engineToken}}"
  body:
    type: json
    data: |
      { "title": "bruno Indonesian client balance retrieval" }
runtime:
  scripts:
    - type: tests
      code: |
        test("201", function () { expect(res.getStatus()).to.equal(201); });
        bru.setVar("indonesianClientBalanceSessionId", res.getBody().data.id);
