info:
  name: healthy semantic no-match is disclosed as out of scope
  type: http
  seq: 4
http:
  method: get
  url: "{{baseUrl}}/chat/jobs/{{retrievalHealthyJob}}/response"
  headers:
    - name: authorization
      value: "Bearer {{retrievalHealthyToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        // FIN-138 — poll berbasis hitungan (FIN-134) habis dalam ~puluhan ms
        // karena setNextRequest mengulang tanpa jeda `--delay`, jauh lebih
        // cepat daripada panggilan embedding eksternal yang sesungguhnya.
        // lib/poll.js sudah dipakai FIN-141 untuk masalah yang sama: batas
        // WAKTU (90 s) dengan tidur per-poll.
        const poll = require("./lib/poll.js");
        if (await poll.awaitJob(bru, req, res, () => true)) {
          const document = res.getBody().data;
          test("FIN-42: healthy semantic no-match is out_of_scope", function () {
            expect(res.getStatus()).to.equal(200);
            expect(document.kind).to.equal("limitation");
            expect(document.outcome).to.equal("Unsupported");
            expect(document.completeness).to.equal("Unknown");
            expect(document.completeness_reason).to.equal("out_of_scope");
          });
        }
