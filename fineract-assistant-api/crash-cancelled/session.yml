info:
  name: session untuk yang dibatalkan saat attempt Running
  type: http
  seq: 2
http:
  method: post
  url: "{{baseUrl}}/chat/sessions"
  headers:
    - name: content-type
      value: application/json
    - name: authorization
      value: "Bearer {{crashCancelledToken}}"
  body:
    type: json
    data: |
      { "title": "FIN-141 yang dibatalkan saat attempt Running" }
runtime:
  scripts:
    - type: tests
      code: |
        expect(res.getStatus()).to.equal(201);
        bru.setVar("crashCancelledSession", res.getBody().data.id);
