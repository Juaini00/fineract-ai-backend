info:
  name: cancel saat attempt Running
  type: http
  seq: 5
http:
  method: post
  url: "{{baseUrl}}/chat/jobs/{{crashCancelledJob}}/cancel"
  headers:
    - name: authorization
      value: "Bearer {{crashCancelledToken}}"
runtime:
  scripts:
    - type: tests
      code: |
        test("OVR-6.4 (FIN-141): cancel atas job Running → Cancelling", function () {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.lifecycle).to.equal("Cancelling");
        });
