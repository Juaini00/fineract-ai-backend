[package]
name = "chat"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
# Lihat crates/app/Cargo.toml: nama `core` pada extern prelude menutupi std `core`.
foundation = { path = "../core", package = "core" }
